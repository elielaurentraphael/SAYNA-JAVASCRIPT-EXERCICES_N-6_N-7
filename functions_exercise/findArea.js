// findArea.js
// Écrivez une fonction "findArea" qui reçoit 2 paramètres (6,9) et calcule l'aire du rectangle.

function findArea(x, y) {
    return "L'aire du rectangle ayant une longueur de " + x + "m, de largeur " +
        y + "m est de : " + x + " x " + y + ", soit : " + x * y + "m² .";
};

console.log(findArea(5, 9)); // 45 m².